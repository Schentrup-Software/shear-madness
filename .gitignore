.DS_Store
/node_modules/

# Local PocketBase instance (devcontainer)
/.local-pocketbase/
/.env.development.local

# Server-side secrets (Google OAuth client ID/secret) — never commit
/.env
/.env.local
/*.apps.googleusercontent.com.json
/client_secret*.json

# React Router
/.react-router/
/build/

/test-results/
